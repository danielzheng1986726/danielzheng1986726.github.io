# AGENTS.md — daniel-site

先读 README.md。改内容只动 `src/content/`，改样式只动 `src/styles/global.css`。

硬规矩：
1. 首页不放数字读数。
2. 不写公司名、不写年份、不写在职状态。数字口径以 yage 仓库 capability-audit 已核实的为准。
3. `git push` 等于发布，先问 Daniel。
4. 文章上站前过 writing-skill 的 lint 和冷读。
